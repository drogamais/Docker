name: 'dbt_projetos'
version: '1.0.0'
config-version: 2

# Nome do perfil que você configurou no seu arquivo profiles.yml
profile: 'dbt_projetos' 

# Onde o dbt deve procurar os arquivos
model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

# CONFIGURAÇÃO DOS MODELOS
models:

      